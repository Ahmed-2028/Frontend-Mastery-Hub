// Define three variables with numerical values (grades)
let math = 18.5;
let logic = 20;
let programming = 19.2;

// Use Math.max to find and print the highest number among the three
console.log(Math.max(math, logic, programming)); 

// Use Math.min to find and print the lowest number among the three
console.log(Math.min(math, logic, programming)); 

// Use Math.round to round the math grade to the nearest whole number
console.log(Math.round(math));
